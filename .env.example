# App Configuration
NEXT_PUBLIC_ADMIN_EMAIL=""

# EmailJS Configuration
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=""
NEXT_PUBLIC_EMAILJS_SERVICE_ID=""
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=""

# Real SMTP Email Configuration (Nodemailer for Gmail / Outlook)
SMTP_USER=""
SMTP_PASS="YOUR_16_CHAR_GMAIL_APP_PASSWORD"
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="465"

# Database Configuration (PostgreSQL - Neon / Vercel Postgres)
# Pooled connection string (auto-injected by the Vercel Storage integration)
DATABASE_URL=""
# Direct/unpooled connection string (local fallback)
DATABASE_URL_UNPOOLED=""
